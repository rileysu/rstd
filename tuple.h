#define tuple1(AP) struct { AP a; }
#define tuple2(AP, BP) struct { AP a; BP b; }
#define tuple3(AP, BP, CP) struct { AP a; BP b; CP c; }
#define tuple4(AP, BP, CP, DP) struct { AP a; BP b; CP c; DP d; }
#define tuple5(AP, BP, CP, DP, EP) struct { AP a; BP b; CP c; DP d; EP p; }
#define tuple6(AP, BP, CP, DP, EP, FP) struct { AP a; BP b; CP c; DP d; EP p; FP p; }
#define tuple7(AP, BP, CP, DP, EP, FP, GP) struct { AP a; BP b; CP c; DP d; EP p; FP p; GP p; }
#define tuple8(AP, BP, CP, DP, EP, FP, GP, HP) struct { AP a; BP b; CP c; DP d; EP p; FP p; GP p; HP p; }
